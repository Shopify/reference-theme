<div
  class="shopify-block-{{block.type}}"
  {{ block.shopify_attributes }}>
  {{ block.settings.liquid }}
</div>

{% schema %}
{
  "name": "Custom liquid",
  "tag": null,
  "settings": [
    {
      "type": "liquid",
      "id": "liquid",
      "label": "Custom liquid",
      "default": "<div></div>"
    }
  ],
  "presets": [
    {
      "name": "Custom liquid"
    }
  ]
}
{% endschema %}
